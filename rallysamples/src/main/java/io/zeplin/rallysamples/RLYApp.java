package io.zeplin.rallysamples;

import io.zeplin.rally.application.CoreBaseApplication;

/**
 * Extend your application class to {@link io.zeplin.rally.application.CoreBaseApplication} instead of {@link android.app.Application}
 * To be able to use the Rally connection instances directly
 */
public class RLYApp extends CoreBaseApplication {
    // Application related methods
}
